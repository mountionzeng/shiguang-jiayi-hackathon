# 拾光 AI 项目交付约定

## 工作入口与部署版本

- 先读 `docs/WORKSPACE.md`。仓库根目录是统一主线入口；`DK—小程序` 是资质/材料目录，不是本仓库。
- 开工先核对当前路径、分支和状态，运行 `npm run workspace:check`。该检查对照已记录云端快照，不代表实时查询云端或功能已验收。
- 归档工作树和 `refs/archive/` 仅用于恢复；不要从旧副本继续开发或部署。未完成的记忆共创功能保留在 `codex/send-image-integration`，不能当作正式验收版本。
- 部署使用 `scripts/wechat-cloud-deploy.mjs`，执行前要求工作树干净、包含最新本地主线、AppID 与云环境匹配。不得绕过检查直接从有未提交修改的副本部署。
- 先固定部署代码提交，再记录函数名、环境、提交与验证结果。仅显示 Active 不代表源码与本地一致；需要时下载比对业务源码。未经授权不得开启发布/权限开关。
- 局部测试使用 `npm run test:files -- tests/具体文件.test.js`；完整检查使用 `npm run check`。`npm test -- 文件名` 不会排除脚本内的全量测试。

## 给用户预览二维码之前

用户于 2026-09-23 明确要求：每次提供二维码前，先在电脑上试完一个整体流程。允许必要的算力消耗，也允许在现有文章中临时增加测试文字，测试结束后必须清理。

- 每次交付新二维码，都先用本次待交付代码在微信开发者工具中跑通本次改动涉及的完整用户流程。仅通过单元测试、查看页面、注入展示数据或生成二维码，不算完成验收。
- 对文字 AI 与记忆入文流程，要实际走完输入、连续追问、整理保存、选择已有故事与章节、查看原文、生成或插入预览、确认写入、重新打开核对结果；覆盖本次交付涉及的分支。涉及语音时，电脑端先验证可完成的录音与转写链路，明确区分仍需真机验收的部分。
- 必要时进行真实 AI 调用，遵守用户当前有效的费用授权与上限；不得把模拟结果描述为真实模型验证。额度不足或真实调用未完成时，明确说明尚未验收。
- 优先使用可识别的虚构测试文字。可在用户现有文章上验证真实写入，但先记录目标与原文基线，仅改动本次测试需要的部分，不删除或覆盖用户原有内容。
- 测试完成后，通过正常编辑/恢复流程清理本次新增文字及临时测试内容，重新打开核对清理结果。不永久删除用户数据或历史版本；若期间用户或其他任务也修改了文章，不可整篇恢复旧快照覆盖新改动，应比对后只撤回本次测试增量。
- 任一步失败，先修复并重跑相关完整链路。若有必须由用户完成的步骤或外部阻碍，清楚报告未完成的环节，不把二维码作为已通过验收的交付。
- 验收与清理完成后，再生成对应代码版本的二维码；交付时简要说明实际跑过的流程及任何真机验证限制，并在交接文档记录结果。测试后若改了影响流程的代码，重新验证受影响链路。

该约定适用于所有在本项目协作的任务。
