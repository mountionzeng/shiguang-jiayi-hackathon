# 故事共创与跨端验收记录（未完成）

用户要求：全部完成，先在电脑验证，再给新的小程序二维码。本记录不等于上线或验收通过。

## 本次补齐

- 电脑端故事卡片选择、预览和 PNG 下载；采用同一权威来源/审核描述，不接收浏览器正文。下载前重新核验，关闭/切换后取消旧响应。
- 权威 `/v1/story/card` 及双仓 1.5.0 契约；云函数增加内容审核权限声明。
- 小程序卡片：修正勾选样式、长图静默裁切、隐藏时的私密数据清理、异步保存取消、照片比例和画布实例参数。
- 未部署新云函数时，卡片先检查 capabilities，显示尚未开放而不是旧服务的“请求编号无效”。
- 卡片服务增加来源空间灰度、阅读许可和 HTTPS URL 校验；鉴权前不报告版本冲突。
- 电脑保存期间禁止继续改段落；切换故事后迟到保存响应不能覆盖当前故事。

## 已有证据

- 电脑端全量：547 个文件通过、11 个跳过；4679 项通过、57 项跳过。生产构建通过，仍有现有大 bundle 警告。
- 小程序类型检查及 657 项全量测试通过，`git diff --check` 通过；覆盖旧云版本拒绝、离页取消保存、原生 page.json 字段、内容审核权限声明等。电脑端 547 个测试文件、4679 项测试、215 项小游戏测试通过，类型检查、功能账本、迁移基线和生产构建通过。
- 微信开发者工具打开的是 `release/2026-09-16-ai-photos` 工作树，AppID 为 `wx6be512f0fe129b62`，云环境为 `cloud1-d0g8c8yg0513a6068`。新部署后的单账号共享 capabilities 已可用。
- 自动化启动出现 simulator timeout，Cmd+R 普通重新编译后首页正常出现；不能据此声称已找到启动超时的根因。
- 卡片分包实际可以打开。真实卡片预览最初被内容安全检查拒绝；独立 `contentSecurityCheck` 对同样内容返回 `pass`，最终定位到 `storyBooks/config.json` 未声明 `security.msgSecCheck` 云调用权限。补齐唯一所需权限并重新部署后，真实模拟器已成功生成最终图片预览；没有调用“保存到相册”，未写入用户相册。
- 在真实故事 `story-t-id5kpg1kqu352` 上完成主人单账号邀请流程：正确读取故事“壮”和章节“壮壮”，编辑与复制能力均可选，邀请有效期提示为 24 小时，分享路径不包含正文。测试产生的待申请邀请已通过权威云函数撤销。
- 电脑端实现已快进合入 `drinking-time-local/main`（提交 `f1507a81`）。`http://localhost:3000/stories` 实际运行并显示“微信里的故事”及未绑定空状态，明确说明编辑的是同一本故事、不会另存第二份。
- 三个共享页面误写的 `enableShareAppMessage` 导致原生配置警告；删除后重新编译该警告消失。仍观察到既有本地字体加载错误及 story-switcher 选择器警告，尚未修改这些模块。

## 当前阻塞

### 电脑端高风险代码审查

- 已完成授权、HMAC、迁移、API 契约、可靠性、异步界面、性能和部署视角的分层审查。
- 可安全自动修复的项目已落在电脑端提交 `3c1d6124`：严格拒绝双载荷降级、统一卡片照片编号和文字总预算、拒绝重复选择、收紧修订编号与 MySQL `INT` 边界、更新 25 个迁移基线，并在拒绝异常权威响应时取消剩余响应流。
- 修复后完整 `pnpm verify:merge` 通过：548 个测试文件、4683 项测试通过，57 项外部 MySQL 测试按现有条件跳过；215 项小游戏测试、类型检查、功能账本、25 项迁移基线和生产构建均通过。构建仍只有既有 bundle 大小警告。
- 后续高优先级修复已落在电脑端提交 `389e08ef`：冲突只重放相对旧基础真实修改的本地段落，同段双方修改或远端删除本地区块时失败关闭并保留草稿；保存覆盖写入和读回确认全过程，只有读到回执对应修订才显示成功，未保存文字可从合并写入的 `sessionStorage` 恢复。
- 照片只在接近视口时请求，故事卡最多四张；未配置权威端时不接受授权绑定，旧授权不能回退较新绑定，权威撤权会使电脑端本地绑定失效。
- 新增迁移 `0025_dry_sunset_bain` 和 `shiguang_bridge_nonces`，nonce 以数据库原子领取支持跨进程和进程重启防重放；过期记录清理限频，不再给每个桥请求增加一次全表范围写操作。
- 双仓协议已同步到 1.5.1，并新增权威端上线/回滚文档，明确先迁移、再部署默认关闭的消费者、配置 HTTPS 与密钥、最后启用生产者的顺序。
- 最终完整 `pnpm verify:merge` 通过：549 个测试文件、4693 项测试通过，57 项外部 MySQL 测试按现有条件跳过；215 项小游戏测试、类型检查、功能账本、26 项迁移基线、生产构建和环境门禁均通过。构建仍只有既有 bundle 大小警告。

之前的锁屏和管理入口阻塞已解除：通过原微信开发者工具的已登录云控制台进入旧 `cloud1-d0g8c8yg0513a6068`，不再走网页关联账号流程，不切换 AppID。

2026-09-19 后续进展：

- `user_accounts` 已收紧为“小程序所有用户不可读写”；与 20 个 `story_*` 专用集合一起在真实模拟器只读查询时均返回云数据库 `-502003 Permission denied`。这证明小程序客户端不能直接读取这些服务端专用集合。
- 已逐一创建规则模板中的 20 个 `story_*` 专用集合（不包括已存在的 `user_accounts`），创建时选择“所有用户不可读写”。控制台集合列表已确认全部存在，最后的 `story_invite_rates` 规则也显示该选项。未写入测试故事或身份映射，未修改现有故事正文。
- 原环境一次规则查询出现 `tunneling socket ... statusCode=503`；重新查询后恢复。不是账号不存在，也没有改变代理设置。
- 新增 `scripts/package-story-desktop-access.mjs`、隔离测试及 `deploy/story-sharing/desktop-package.md`。只打包 9 个可达源码文件与 package/config；SDK 版本由 `latest` 收敛至与 storyBooks 一致的 `~3.0.1`。打包测试通过，`storyDesktopAccess` 已部署。
- 开发者工具自动化端口 `9421` 与 CLI 端口 `14220` 均正常。上次超时来自把 20 个云数据库请求串行塞进一次求值；拆开连接检查并在页面内并发请求后验证成功。
- 小程序实际运行页 `pages/index/index` 对 20 个新 `story_*` 集合逐一执行只读查询，全部返回云数据库 `-502003 Permission denied`。这证明客户端不能直接读这些专用集合；测试没有请求写入，也没有读取或修改现有故事正文。
- 用户明确选择：暂不安排第二个微信，先完成电脑端和单账号验证。双身份/真机验证继续标为未完成，不能借单账号结果声称已经通过。

数据库新集合的客户端只读拒绝已经取得真实运行证据。存储自定义前缀规则因当前免费套餐明确拒绝配置，尚未达到理想规则；现阶段采用云函数生成副本、128 位随机路径、私有资产记录、签名 URL 前重新鉴权、TTL 不超过 300 秒，以及客户端同前缀上传文件不进入读取链路的补偿控制。未升级套餐、未付费。

## 仍需完成，不能用二维码代替验收

- 核对邀请公开发布授权、受限阅读照片显示、副本再分享等尚未贯通的入口与服务器能力。当前公开授权仍默认拒绝，不能标记整个 U1–U8 完成。
- 为 `storyDesktopAccess` 配置公网 HTTPS 路由，再为双方配置同一强随机共享密钥和默认关闭的开关。在路由未成立前不得启用 `STORY_DESKTOP_ACCESS_ENABLED` 或 `SHIGUANG_STORY_AUTHORITY_ENABLED`。
- 完成真实“小程序短码 → 电脑绑定 → 权威故事读取/保存 → 小程序版本更新”全链路，以及冲突、撤权、本人新增经历回传、照片保留验证；不覆盖现有故事。
- 使用第二个微信身份验证受限章节、跨账号拒绝、撤权后失效、亲友转发和副本再分享。用户目前无法提供第二身份，所以这些项目明确保持未完成。
- 免费套餐下继续核验复制素材的私有路径与实际 TTL/CORS；不能把补偿控制描述为已配置理想存储前缀规则。
- 微信官网 OAuth 的平台资质未验证，短码不称为官方扫码登录。双身份与真机结果也不能用内存仓储测试代替。
- 完成当前可做的代码审查和单账号/电脑端验证后，再使用开发者工具生成新预览二维码。二维码只代表预览构建，不代表上述双账号和 HTTPS 权威链路已验收。未提审、未发布社交内容、未调用付费 AI、未迁移新小程序 `wx86ae3e9d507ce52d`。
