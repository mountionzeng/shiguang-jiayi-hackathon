# 小程序性能：开发基线与分批改动

日期：2026-09-24。分支：`codex/miniprogram-performance`。初始基线：`main@b22c7d2`；第二批已同步自动保存修复 `main@b9957ec`。

这是本地代码、合成数据与开发者工具的验证记录，**不是完整性能问题的验收，也不是真机耗时报告**。本批未部署、未提供预览二维码、未改真实用户内容。

## 环境与协作

- 工作树：`/Users/yuandai/Documents/New project/shiguang-jiayi-hackathon/.worktrees/codex/miniprogram-performance`。
- 仓库根目录仍是旧迁移分支及其他任务的脏工作区；未改动它。
- 主线整合任务确认当前基线 `b22c7d2`，旧数据迁移已完成，不应重跑。
- 第一批避开文字编辑任务的 `book.ts`。待其自动保存修复合入 `main@b9957ec` 并释放开发工具后，第二批在此基础上修改照片加载，保留草稿账号验证、编辑中刷新保护、版本锁与保存序列。
- 图片任务确认没有正在改动的图片或代码；其重复 AI 角标修复已在本批基线中。
- 原交接文档记录的 282 项测试已过时：修改前 `npm run check` 实际为 **829 通过 / 0 失败**。

## 已实现

1. `storyShelf` 和 `memoryPlacements` 在每次计算内建立书稿版本索引，按故事/档案取小集合，继续调用已有版本选择与校验逻辑。素材有效性用 Set 查询；摘要仍按原素材数组顺序选取，保留记忆 ID 顺序及重复值。
2. 云端 `storyCommand` 去掉未使用的写前全量房间读取。仍由服务端执行命令、校验权限和版本；成功后重新读取一次权威状态，失败仍抛错。本地模式继续读取状态再应用命令。
3. 加入 `[performance]` 控制台与实时日志：`room.load`、`room.cloud`、`story.shelf`、`book.refresh`。记录页面路径、耗时、成功/失败、条数；云端读取记录服务路径、降级原因、客户端分页请求次数。不记录正文、账号 ID、房间 ID、指纹或原始错误。日志 API 不可用时不影响加载。
4. `book.ts` 本地照片按 ID 去重、最多四张并行读取，同时开始读取云端插图列表。路径解析完毕后仍按原文顺序构建映射；缺失照片继续保留可恢复引用，所有读取结束后仍检查刷新序号、卸载、编辑中和选图状态。

索引只属于一次同步视图计算；没有跨页面缓存、没有更改指纹算法，也没有改动鉴权与迁移代码。

## 合成数据前后对比

环境：本机 Node.js v24.18.0。命令：`node_modules/.bin/tsx scripts/benchmark-room-views.ts`。
测量对象：`storyShelf` 加 `memoryPlacements`；预热 5 次，测量 25 次，每轮验证输出一致。

| 数据规模 | 修改前中位数 / P95 | 修改后中位数 / P95 |
|---|---|---|
| 20 本书、200 条记忆、400 个版本 | 0.272 / 0.325 ms | 0.106 / 0.135 ms |
| 100 本书、2,000 条记忆、10,000 个版本 | 16.930 / 20.516 ms | 1.203 / 1.470 ms |

两组数据前后输出的 SHA-256 分别保持不变：

- `7099206e60baa322d75e461596e1576a9f57696fe53c0c0ea48da8e9b18a0317`
- `a704ba5c7be23df8411cb24784a0f1e8290f16390d02f9582c404e20ecde8065`

该结果证明本地计算量降低，不代表真实用户房间的数据规模或手机整体打开速度。网络等待未计入。

## 验证结果

- 第一批 `npm run check`：834 通过；合入自动保存后第二批最终检查：**847 通过 / 0 失败**，类型检查通过。
- 本任务新增 7 项回归测试：索引与原版选择器一致、明确指定旧版本、同日版本排序、旧档案回退、缺失/删除版本报错、下一次计算刷新数据、摘要与素材顺序、章节归属；云端写入调用顺序与失败边界；降级分页计数与日志隐私；日志失效不影响读取；九张照片由九轮串行等待变为三批并行且结果顺序一致；页面读取竞态保护。
- 云端命令边界测试由真实客户端仓库/服务代码配合模拟微信 I/O 运行：从原来的 `state → update → state` 减为 `update → state`，写入失败不读取状态，云端未就绪不发送写入，写后读取失败仍抛错。
- `git diff --check` 通过。人工核对索引未持久化、未改原数组、没有将云端读取失败转换为空状态。

## 交接文档中需要纠正的判断

`loadCloudRoomState` 已优先使用 `storyBooks.state`。版本 1 响应不完整时会抛出错误，**不会静默回落客户端读取**；旧版响应和允许的服务缺失错误才进入降级路径。本批测试覆盖了这个边界。

## 开发者工具真实只读流程

隔离预览：`/private/tmp/shiguang-performance-preview-pimj_o2v`。企业 AppID 与云环境沿用仓库配置，AI 发布开关未改变。官方工具门禁确认版本匹配、登录有效。

实际走完：首页 → 人生之书 → 点一本书 → 打开整理好的章节 → 点章节查看正文 → 返回 → 再次打开。书稿加载 5 个章节、1 张背景图，未报告加载错误；没有输入正文、生成图片、调用付费模型或清理存储。截图已查看，保存在本机 `/private/tmp/shiguang-performance-shelf.png` 和 `/private/tmp/shiguang-performance-chapter.png`，含用户内容，不提交仓库。当前书无本地正文照片，因此四路本地照片读取由专项测试验证，尚未在真实手机照片上验收。

这次样本中的房间有 3 位成员、7 条记忆、23 个书稿版本；7 条故事记录中书架显示 2 本可用故事。

| 环节 | 房间云端读取 | 整页书稿 refresh |
|---|---|---|
| 编译后首页 | 8,004 ms | — |
| 进入书架 | 5,208 ms | — |
| 打开书稿 | 3,753 ms | 9,444 ms |
| 返回后书架重读 | 4,144 ms | — |
| 再次打开书稿 | 3,691 ms | 7,794 ms |

所有这些读取均为 `route=story-service`、`clientPageReads=0`；书架计算为 0–1 ms（Date.now 分辨率）。这证明此样本的秒级等待主要在云端读取及书稿加载链路，不是客户端每页 20 条的降级分页。`room.cloud` 包含身份解析与云函数调用，`book.refresh` 还包含草稿身份验证和图片列表；目前不能进一步把时间归因到某一次鉴权或某张图片。上述是修改后的单次开发工具观测，不是改前/改后的真机对照，也不是统计分位数。

## 第三批：目录和纯文字先显示

新增 `room.identity`、`room.state`、`book.identity`、`book.photos`、`book.images` 分段日志，继续只记录固定操作名、页面路径、成功/失败与耗时。身份检查的次数、顺序与账号映射均保持不变。首次 `currentFamilyId` 还涉及账号初始化，不能仅因耗时较长就删除；草稿继续使用新鲜身份验证，避免跨账号恢复。

目录和纯文字章节在房间状态及草稿身份验证完成后即可显示，背景图与未打开章节的照片继续在后台读取。含正文照片的章节仍等路径与引用映射准备好后才灌入原生编辑器。迟到的图片仅补映射和底图，不重设编辑器正文；新插入的照片映射优先保留。打开章节时检查刷新序号、输入序号、卸载、面板与最新章节选择，返回目录会取消未完成的章节打开。

### 同一本书的开发工具对照

改前为 `eeb24a6` 的阻塞式渲染加本批分段日志，改后为本批先显示目录的实现；同一隔离预览、同一选中故事，没有更改服务端或用户正文。两次均为内部 8 个章节、目录可见 5 章、3 个正文照片引用、0 个解析成功的正文照片路径、1 张已解析底图。此前记录的“无本地正文照片”应理解为没有解析出可用照片，本次确认书稿仍有照片引用，不能当作照片已真实验收。

| 环节 | 改前单次 | 改后单次 |
|---|---:|---:|
| 书稿页房间身份（已有缓存） | 1 ms | 1 ms |
| 云端 `storyBooks.state` | 4,529 ms | 4,368 ms |
| 房间读取总计 | 4,532 ms | 4,372 ms |
| 草稿身份验证 | 1,103 ms | 1,193 ms |
| 云端插图列表 | 2,331 ms | 1,454 ms |
| 正文照片路径读取 | 2,438 ms | 2,503 ms |
| `book.refresh` | 8,092 ms | 5,592 ms |

改后日志中 `book.refresh` 先于 `book.images` 和 `book.photos` 完成，目录准备不再等待约 2.5 秒的图片读取。两条图片路径并行，不能相加；网络波动不能算成代码收益。`book.refresh` 现在表示初始目录/章节准备完成，不表示所有图片就绪，也不是实际首帧绘制耗时。此处是单次开发工具样本，不是统计分位数或真机性能承诺。

冷启动首页身份解析改前 4,752 ms、改后 4,939 ms；状态读取改前 4,098 ms、改后 4,569 ms。冷启动和云端状态读取仍慢，本批没有解决这个剩余瓶颈。所有样本仍为 `story-service`、`clientPageReads=0`。

### 第三批验证

- `npm run check`：类型检查通过，**851 通过 / 0 失败**。新增 4 项测试，覆盖目录不等图片、图片未完成时输入不被覆盖、含照片章节直接打开等待映射、较新章节选择与返回目录取消旧请求、保留新插入照片；原有刷新被替代/卸载/编辑保护测试继续通过。
- 在隔离开发工具实走：打开同一本书 → 点击纯文字章节 → 点目录 → 重开章节 → 返回上页 → 重开书稿；目录与正文截图已查看，无加载错误，没有输入或保存用户正文。截图仅存本机 `/private/tmp/shiguang-render-first-contents.png` 和 `/private/tmp/shiguang-render-first-chapter.png`，不提交仓库。
- 自动保存、账号隔离、草稿恢复、冲突与版本锁依赖现有回归测试；本批未实测真实写入流程。含正文图片的原生编辑器时序由受控测试覆盖，当前真实样本没有可用正文照片路径，仍需真机补验。

## 第四批：服务端分段计时与一轮独立查询并行

### 代码证据与边界

`storyBooks.state` 原先在完成身份解析、读前 owner 校验之后，先单独等 `families`，再并行读 `stories`、`biography_drafts`、`story_migration_items`、`memories`、`family_members`，最后再次校验 owner。五个集合原本就并行，不应误判为逐集合串行。首次 identity bootstrap 和已有关联身份的路径也不同，不能拿后者替代首次登录验收。

本批只将 **state 的房间资料与五个集合同时读取**；查询仍使用服务端身份得到的稳定 familyId。缺失房间、任一集合读取失败都拒绝整个结果，不返回空状态。命令、迁移和 AI 上下文的房间读取顺序保持原状；未改身份实现、事务、迁移映射、权限开关或返回结构。并行组最多由 5 个读取增至 6 个，实际服务器收益和负载仍待观察。

### 新日志

云函数内新增 `[performance]`，每条只有固定 `operation`、`outcome`、`durationMs`：

- `state.identity`：解析/首次建立 alias、principal、space 身份，包含身份事务。
- `state.authorize.before` / `state.authorize.after`：读取前后的 owner 复核。
- `state.query.families`、`state.query.stories`、`state.query.biography_drafts`、`state.query.story_migration_items`、`state.query.memories`、`state.query.family_members`：各次读取；集合计时包含服务端分页全部读取。
- `state.load`：上述并行读取及状态组装。
- `state.total`：整个 state 分发，包含上述三个权限阶段；不包含云函数冷启动、网络传输及平台响应序列化。

日志使用云平台本身的请求上下文分组，不额外输出账号、家庭、故事、正文、查询条件或原始错误。日志失败不改变原始返回值或异常。`state.query.*` 相互重叠，不能相加；`state.load` 已包含它们，不能与其相加。启用身份权限时正常请求共 11 条日志，失败时只记录已执行阶段。权限功能关闭时保持旧路径，省略身份和 owner 三条日志。

### 受控证据与检查

先写测试，把房间读取挂起，确认改前只启动 `families`，其余五类尚未开始；改后六类全部启动，释放等待后完整返回内容一致。

合成对照：`node scripts/benchmark-story-state.cjs`，模拟已建立身份的账户、7 本故事与 7 条记忆，每次数据库读取附加 20 ms 延迟，3 次样本。改前耗时 303.71 / 303.81 / 301.29 ms，改后 280.28 / 273.66 / 283.28 ms；这只是本机人工延迟实验，不是生产或真机速度。

- 前后每次仍为 18 次数据库读取；其中 alias 4 次、principal 3 次、space 3 次、family 3 次、集合读取 5 次，权限检查未省略。
- 单页集合条件下，依赖链从 14 轮等待减为 13 轮；数据读取阶段从两轮减为一轮。单轮实际耗时因数据库与网络而异，不能据此推断线上 4.4 秒会降至多少。
- 六次返回内容 SHA-256 均为 `ce0ae5987f313a608010dd65eeb669fe4fcf336737bd2ed5fed8e82c3d1e6f4b`。
- 类型检查及完整测试：**857 通过 / 0 失败**。新增 6 项，覆盖并行启动和结果一致、缺失房间/各集合失败、伪造 familyId 与稳定迁移空间、完整身份检查次数、读取中撤销 alias/principal/space/ownership、日志隐私与故障不影响结果；现有首次身份建立、写入版本锁及迁移回归全部通过。

本批仅在独立工作树保存云函数代码，**没有部署，也没有用旧云函数的真实调用冒充新代码回归**。下一次受控部署应完整包含 `storyBooks/performance.js` 及服务/读取改动，不只替换入口；先只读验证首页、书架、同一本书及重开流程，核对响应与撤权行为，并将客户端 `room.state` 与同一云请求的上述阶段对应。真实新账号、双端手机与真实写入验收仍未完成。不能把本批标记为云端 4.4 秒问题已解决。

## 剩余优化与验收

下一步在受控部署后用新增分段日志定位 `storyBooks.state` 内部数据库与鉴权阶段的真实耗时，并检查新账号初始化路径。完整验收还需补“整理记忆 → 切标签”、首次登录、iOS / Android 真机及修改前后同流程对照，才能给出整体提速结论。

日志检查：实时日志搜索 `[performance]`，按 `operation` 和 `page` 比较。`room.cloud.route=story-service`、`clientPageReads=0` 表示走完整云函数状态；`client-fallback` 必须核对原因；分页次数仅统计客户端集合分页尝试，不包含身份查询、单文档查询和云函数内部数据库读取。`room.load` 已包含 `room.cloud` 的耗时，两者不能相加。

尚未实施的交接项：

- 已完成四路照片读取和目录/纯文字先显示；含正文照片章节继续等待映射，尚未实现安全的正文图片渐进填充。
- 插图压缩及主包出码体积测量。
- 指纹重复计算优化：先验证状态是否不可变，不能直接用对象身份缓存可变状态。
- 房间跨页面缓存、标签切换重读与批量删除重读：需要明确写后失效、账号切换和撤销权限的语义。
- 当前开发者工具账号已确认走完整云函数读取；其他账号/真机、鉴权开关和云函数规格仍待确认，没有改变鉴权校验次数。

## 后续验收观察

本批整合后，由性能任务负责在开发者工具及双端真机的上述流程中检查耗时、请求数和错误。重点看保存后的内容是否立即可见、旧版本是否保持、首次登录是否正常、刷新失败时旧内容是否保留。发现版本/内容不一致应停止交付并回退本批独立提交；不能仅凭本地测试提供已验收的二维码。
