# 从记忆保存页进入已有故事，并在章节中插入

## 用户反馈与实现

记忆整理保存页只从记忆的故事标签生成选项，已有独立故事或只保存过书稿的故事不会出现。现在复用书架来源，按稳定故事标识选择目标，显示章节数；空故事也可选，亲友受保护副本不作为整篇写入目标。改成未整理片段或输入新故事名时，清掉旧目标标识，防止误关联。

选已有故事后按钮变为“保存并选择章节”。保存成功后携带同一条记忆标识进入该故事的章节选择；导航失败保留“继续选择章节”，不会再次创建记忆。选择故事本身不写章节正文。家庭提交沿用原流程。

选中既有章节立即显示可滚动原文。默认“保留原文，插入这段记忆”，通过原生选择窗口选开头、任一段后或末尾。段落边界保留原字符串偏移，插入不会规范化原文或移动照片；其他章节和原有记忆引用保留。已写进章节的记忆不再默认勾选。

下一步展示可编辑的新增文字与插入后的全文；确认后保存新版本，可撤回。既有待确认修订、失效位置、空新增文字和版本冲突均阻止写入。AI 文字标识沿用记忆/章节来源，不把已经整理过的 AI 文字标成人工原文。

AI 共创故事额外提供“让 AI 结合本章重新衔接”：复用已接通的生成服务，发送整章原文与所选记忆。客观记录故事不提供 AI 重写；长文的 AI 重写长度限制不影响本地插入。既有身份、授权、服务端来源和版本保护继续生效。

## 验证

- 类型检查通过；全部 **818 项测试通过**（新增 8 项）。
- 新增覆盖：独立空故事与旧书稿选项、保存后按稳定 ID 继续且无重复记录、清除旧目标、中间段落插入保留照片与换行、失效位置与空文本、修改新增文字、跨章节引用不丢失、并发版本冲突、不默认勾选已写入记忆。
- 既有 AI 整理回归显式选择 AI 衔接，继续验证旧文作为上下文、保留照片与撤回。
- 开发者工具实际读取现有故事：保存页可见已有故事及章节数，选中后显示“保存并选择章节”；章节页展示原文和原生段落位置选择器，固定下一步按钮可见。
- 全文预览布局使用仅存在于页面内存的虚构文字检查，未保存测试记忆或改写真实章节。实际确认写入由本地测试验证，手机端真实写入待用户试用。
- 本轮没有付费模型请求，也没有新部署云函数。开发者工具首次自动化连接超时、一次导航 AppID 校验遭遇 TLS 网络错误；转用已打开的界面完成核对，后续截图工具成功，控制台错误过滤结果为空。

## 预览

隔离目录：`/private/tmp/shiguang-chapter-insert-preview-20260923`。

只有该目录开启 `CLOUD_AI_RELEASE_READY`；仓库仍为 false。企业 AppID 和环境沿用已确认配置，personalMemory 仍关闭。截图只留本机，不进仓库；包含本人正文的截图不得作为公开测试夹具。

验收路径：记录并整理一段记忆 → 选已有故事 → 保存并选择章节 → 看原文 → 选插入位置 → 下一步预览 → 确认写入正文。已有章节的 AI 衔接需该故事使用 AI 共创模式。

新二维码生成成功（`ok=true`、`success=true`，总包 1,864,244 字节）：`/private/tmp/shiguang-chapter-insert-preview-20260923/chapter-insert.jpg`。替代上一版 `ai-flow-fixed.jpg`，尚未公开发布。
