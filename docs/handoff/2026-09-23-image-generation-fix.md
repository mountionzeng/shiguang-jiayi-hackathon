# 配图按钮反馈与真实出图修复

## 已验证

- 基于 main a2f3d89，独立分支 codex/image-generation-fix。
- 真实按钮能够进入授权和提交。首次真实请求在场景提炼阶段 15 秒超时（SCENE_TIMEOUT），没有开始出图。
- storyImages 的 AI_MODEL 仍为 hy3，已在现有云环境更新为 deepseek/deepseek-flash；其他配置保持原值。
- 调整后真实生成第一张插图（225 KB），打开大图，返回书稿后重新进入列表，同一 imageId 仍存在。
- 用户要求缩小标识后，将服务商 footnote 从「图片由AI生成」缩短为「AI生成」，兼容新旧标识的质检提示。再次真实生成一张（188 KB），打开大图确认短标识有效。
- 两张图均保留在用户书内；未新增或改写文章文字。质检分别提示文字/签名瑕疵，未绕过质检或审核。
- 每次提交立即在对应章节显示反馈及按钮 loading；场景失败有明确原因；完成后进度更新为「画好了」。
- npm run check：类型检查及 822 项测试通过；git diff --check 通过。

## 部署

storyImages 已由用户在开发者工具确认两次部署，第二次包含短标识。仅隔离预览开启 CLOUD_AI_RELEASE_READY，仓库门禁保持不变。

## 边界与后续

- 电脑模拟器未复现所有按键完全无响应，已复现并修复真实请求超时与反馈不就近显示。手机触控仍需用户新包复测。
- 当前服务商接口只开放 footnote 文案，字号/透明度由服务商生成，未假称可独立设置。
- 用户随后新增全书封面生成及首页封面同步，另行实现和验收后再交付联合二维码。
- 书稿目录过滤和配图页全量章节的差异已告知主任务，不在本次配图故障中改写历史章节。
- 截图与用户图文仅保留本机临时目录，不进入版本库。
