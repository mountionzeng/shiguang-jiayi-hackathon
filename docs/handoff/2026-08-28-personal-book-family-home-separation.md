# 人生之书／记忆之家叙事边界交接

## 本次产品纠偏

这份文档覆盖旧交接中“全家共同为一位主人公写一本人生之书”的定义。

两个一级入口必须回答不同的问题：

| 入口 | 核心问题 | 叙事人称 | 内容归属 | 是否进入另一入口 |
|---|---|---|---|---|
| 人生之书 | 我这一生经历了什么？ | 我 | 当前用户本人 | 不进入记忆之家 |
| 记忆之家 | 我和家人共同经历了什么？ | 我们 | 家庭共同空间 | 不进入任何人的人生之书 |

这不是文案区别，而是数据和生成来源的硬隔离。

## 已实现行为

- 当前演示身份切换后，每个成员只看到自己的人生之书和个人章节草稿。
- 个人采访使用第二人称问题，引导用户用“我”讲述。
- 个人片段以 `scope: personal` 保存，由本人直接确认；可额外指定一位家人阅读，但归属不变。
- 个人章节只读取当前成员的 `personal` 片段；其他成员的个人片段和所有家庭片段都会被排除。
- 记忆之家只读取 `scope: family + 家庭可见 + 已确认` 的片段。
- 家庭采访引导用户用“我们”讲共同经历，保存后只进入记忆之家待确认队列。
- 底部中央按钮会随当前页变化：人生之书显示“写我的故事”，记忆之家显示“记共同回忆”。
- 原有成员筛选、时间轴、详情、撤下、事实确认和来源追溯能力继续保留。

## 首页随手记与定向分享

- 首页新增“记下此刻”，用于随时写下一句话、一种感受或刚发生的小事。
- 记录采用两步流程：先专心写原话，完成后再选择归入“我的人生之书”或“记忆之家”。
- 归入人生之书时，可以保持仅自己可见，也可以指定一位家人阅读。
- 定向分享只增加阅读权限：接收者在首页“家人分享给我的”查看，不进入接收者的人生之书，也不进入记忆之家。
- 故事主人可以在首页对已有个人片段更改接收者，或恢复为仅自己可见。
- 首页使用可滚动的自定义成员面板，不受微信系统 `showActionSheet` 项数限制；全部个人片段都保留撤回入口。
- 归入记忆之家时不提供定向分享；它仍按家庭记忆规则等待确认，确认后才对全家可见。

## 原生微信小程序实现

核心文件：

- `miniprogram/domain/biography.ts`：`MemoryScope`、个人／家庭素材选择器、个人草稿来源指纹。
- `miniprogram/domain/interview.ts`：个人问题池、家庭问题池和两套追问语气。
- `miniprogram/pages/index/`：当前用户自己的书。
- `miniprogram/pages/quick-note/`：先记录，再选择归属与定向阅读者。
- `miniprogram/pages/room/`：家庭共同记忆时间线。
- `miniprogram/pages/interview/`：通过 `?mode=personal|family` 固定保存去向。
- `miniprogram/pages/book/`：按当前成员隔离个人章节。
- `miniprogram/services/roomStorage.ts`：按成员保存个人章节草稿。

仍然使用 WXML、WXSS、TypeScript 和自定义 tabBar；不要用 React/H5 替换，也不要把 HTML 原型直接嵌入小程序。

## 不能破坏的约束

1. 个人故事绝不能因为定向分享或“家庭可见”参数误传而进入记忆之家。
2. 家庭共同记忆绝不能成为任何成员个人章节的生成来源。
3. 切换演示身份时，个人片段和个人草稿必须跟着身份切换。
4. 旧版缓存中没有 `scope` 的片段按 `family` 处理，不能误判成个人故事。
5. 家庭待确认、拒绝、冲突或私密记录不进入家庭公开时间线。
6. 微信身份、真实邀请、跨设备同步仍未完成，界面必须继续标注为本地演示。
7. 定向分享不属于个人章节来源字段；修改接收者不能让任何个人或家庭书稿失效。
8. 定向分享当前只允许零或一位接收者；异常类型或多个不同成员 ID 的缓存必须失败关闭。
9. 修改分享时必须基于提交瞬间的最新房间状态，不能覆盖面板打开期间新增的故事或章节。

## 当前验证（2026-08-28 最终交接）

- `npm run check`：TypeScript 通过，`48/48` 项测试通过。
- 新增测试覆盖个人故事成员间隔离、个人生成排除家庭记忆、单人定向授权、异常缓存失败关闭、撤回后拒绝读取、旧面板快照不覆盖新内容、超过三条仍可撤回，以及页面级个人／家庭保存和身份切换。
- 微信开发者工具使用真实 AppID `wx6be512f0fe129b62` 正式预览编译成功，体验包 `110.6 KB`（`113300` Byte）。
- 微信模拟器已复核“记下此刻”记录页的中文布局、底部按钮和安全区；最终截图见 `docs/images/06-quick-note.png`。

真机仍需重点检查中文输入法候选栏、整理页长文本滚动、自定义分享面板的多人滚动，以及章节长文滚动。

## 队友继续修改时的口令

> 请先阅读 `docs/handoff/2026-08-28-personal-book-family-home-separation.md`。人生之书只讲当前用户自己的故事，记忆之家只讲用户和家人的共同故事。定向分享只授予指定家人阅读权，绝不改变故事归属或生成来源。任何页面、文案、保存逻辑或生成来源都不得再把两类内容合并。前端必须保持原生微信小程序实现，并继续复用现有队友原型的时间线、成员筛选和阅读结构。
