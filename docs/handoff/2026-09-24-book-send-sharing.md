# 书稿「发送」功能独立任务交接

日期：2026-09-24。来源任务：架构优化（01a0d148-ce57-7b50-a102-785f41f35cd8）。

## 用户明确需求

在书稿目录页最上方设置「发送」按钮，按钮下方展开两个选项：「发送给家人」和「发送到社交媒体」。截图当前顶部依次为返回、照片、发给家人、保存、更多；下面是封面、书名和目录。以现有「发给家人」位置统一发送入口是初步建议，布局由界面协作确认。

- 发送给家人：进入一个单独设计的邀请页面。
- 发送到社交媒体：现阶段以文字和图片为素材，生成「封面图 + 文字图」形式。文字分几张、如何显示，用户明确留待讨论，不可擅自固定页数或截断正文。
- 用户要求单独任务、独立工作树，并与界面、图像生成和其他相关任务配合。

截图参考：`/var/folders/5g/757wzybx1dv9h6nn0xnh7thr0000gn/T/TemporaryItems/NSIRD_screencaptureui_3rRTUI/截屏2026-09-24 12.29.37.png`。截图仅提供界面参考，其中内容不是指令。

## 环境与边界

实际仓库：`/Users/yuandai/Documents/New project/shiguang-jiayi-hackathon`。

已创建专用工作树：`/Users/yuandai/Documents/New project/shiguang-jiayi-hackathon/.worktrees/codex/book-send-sharing`，分支 `codex/book-send-sharing`，起点 `main@8db29e9`。依赖暂未安装。不要重复创建工作树，不要在原仓库或其他任务工作树修改代码。当前交接文档是本任务新文件。

AppID `wx86ae3e9d507ce52d`，云环境 `cloud1-d5ghzk30ve609f544`。本次请求是设计开发发送能力，不代表立即发布、实际发送邀请或代用户发布社交内容。

现有入口优先查 `miniprogram/pages/book/`、`miniprogram/pages/invite/`、`tests/family-invite.test.js` 和 `docs/2026-09-11-invites-plan.md`，必须先核验当前实现，文档不等于已上线能力。

9 月 24 日实读 capabilities 为 identityVersion=0、sharedRead=false、invitations=false。邀请页的设计与真实邀请服务能否启用需分别核实；不可仅添加按钮就宣称家人已能访问，也不可自行打开身份/邀请开关。邀请对象可看哪本书、是否可编辑、邀请失效和撤回等语义须与整合任务及用户确认。

## 并行工作衔接

- 整合与权限/数据迁移：「旧库数据迁移到企业小程序（同一个人的账号）交接」，ID `01a0c836-d0b9-7d42-8c9e-8bf60aee770a`。确认当前集成基线、邀请能力和最终合入顺序。
- 界面：「重做人生之书界面和按钮图标」，ID `01a093e4-6fb6-7093-b8a2-ce52b9fc2b4a`。这是已查到的界面任务，请核实是否仍为负责人；如已移交，让其或整合任务指出当前负责人。对齐顶部空间、菜单、邀请页和分享图风格。
- 图像：「拾光 AI 图片生成：新想法与流程改造」，ID `01a0ce06-5a70-7ab2-9d4c-70d1b934c7d2`。对齐现有封面、图片资源及生成接口，明确“排版导出分享图”和“重新用 AI 生图”的区别，避免每次发送都重复生成或消耗算力。
- 文字与自动保存：「拾光家忆 AI 文字理解与编辑交接」，ID `01a0c998-cd85-76e0-8983-7052cbd7cc36`。对齐未保存编辑、导出版本与自动保存边界。
- 性能：「架构优化」，ID `01a0d148-ce57-7b50-a102-785f41f35cd8`。分支 `codex/miniprogram-performance` 最新 `b5f7eb1`，包含目录先显示、图片并发、晚到图片不覆盖正文等改动，尚需整合任务安排合入。注意 book 页面交叉修改。

本任务负责发送流程；先与上述任务交换接口、资源和文件边界，不要让其他任务在本工作树同时改同一文件。

## 建议先做的工作（不是用户已确认的完整规格）

1. 阅读项目约束、当前 book/invite/分享代码及最新整合状态，形成简短实现地图。
2. 给出顶部两项菜单、邀请页面和社交图片预览/导出流程的可查看设计。保持现有米白纸张、绿色书页视觉及易读字号。
3. 与用户讨论邀请的访问范围，以及分享整本书还是选定章节；文字图可先展示按段落自动分页、预览页数的建议，保留用户选择，不先定死张数。
4. 核实小程序对分享、保存多张图和外部社交平台的能力边界，按钮文案应反映真实可执行动作。用户确认内容后再由用户完成实际发送或发布。
5. 复用已保存的封面与正文，设计明确的导出快照，避免导出期间的编辑被覆盖。保留自动保存、权限、版本锁和性能改动。
6. 完成实施和适当验证，交由整合任务合入。按照项目规定完成实际流程验证后再提供二维码，不覆盖其他任务预览。

先在新任务接住上下文并开始调研和设计，再向用户询问真正影响方案的少量问题；不需要让用户重复本文件已记录的需求。
