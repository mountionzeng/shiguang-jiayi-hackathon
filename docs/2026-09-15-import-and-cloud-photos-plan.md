# 问题九：导入与照片上云（阶段 0 方案，未写代码）

日期：2026-09-15
分支：`feat/import-and-cloud-photos`（从 `main` 的 `d83f1ed` 建立）
状态：**方案稿。** 标「待定」的地方一次问用户一件；标「待对齐」的先和对应会话确认，再写代码。

## 一、已定的决定

| # | 决定 | 来源 |
|---|---|---|
| 1 | 聊天页底部两个「赛后接入」占位按钮去掉，换成一个「导入」 | 协调会话，用户 09-15 |
| 2 | 两个来源：相册或拍照（`wx.chooseMedia`，只有图片）、微信聊天里的文件（`wx.chooseMessageFile`），类型由系统判断 | 同上 |
| 3 | 导入的内容直接存成一条记忆，放进「还没写进书」，不要求先选故事 | 同上 |
| 4 | `.txt`、`.md` 读出正文；Word、PDF 这一版明确提示不支持 | 同上 |
| 4′ | **超过 500 字：存成一条记忆，按顺序分成几段**（用问题四的 `segments`），不再切成几条同标题记忆（1/3） | **用户 09-15 在问题九会话选 B** |
| 5 | 只有图片时请用户写一句话，可以跳过；跳过就存成只有照片的记忆 | 协调会话，用户 09-15 |
| 6 | 所有照片存进微信云开发的云存储，包括书稿里的照片；手机里已有的照片经同意后补传一次 | 同上 |
| 7 | 看图模型帮用户起草那一句话，用户改完再保存；不做文字识别；走腾讯 TokenHub；结果标「AI 生成」 | 同上 |
| 8 | 出图时可以拿这些照片当参考图（问题五主做） | 同上 |
| 分工 | **「看完照片帮用户起草那一句话」转给问题五**，复用它已接好的 TokenHub，看图模型不接两遍。问题九只负责导入与照片上云，并**对外提供统一的「读取照片」接口**（3.7 节）；问题五的看图起草和参考图出图都通过这个接口读照片。先后：问题九先做完照片上云，问题五再接云端照片，之前先用虚构测试图 | 协调会话 09-15，用户已同意 |

边界（协调会话定，必须遵守）：隐私承诺要改并重新提审（问题七把关）；云端照片默认只有本人能读写，家人看照片由服务端核对权限（问题三）；不识别人脸、不判断照片里是谁；AI 能力要等迁到企业主体、拿到「深度合成」类目才能提审，在那之前只在体验版用，**不为了过审把 AI 藏起来**；上传前压缩；主包已 1.58 MB（上限 2 MB），不往包里加大图。

## 二、微信能力核实（开工前查过的）

- **小程序不能浏览手机文件系统。** 小程序只能访问三类文件：代码包里的文件、接口临时生成的文件、`wx.env.USER_DATA_PATH` 下的本地用户文件，按用户和 AppID 隔离。本地用户文件加缓存文件合计上限 200 MB。所以「从手机里选文件」只能走 `wx.chooseMessageFile`，从微信聊天记录里挑。
- `wx.chooseMessageFile`（基础库 2.5.0+）：`count` 为 0–100；`type` 可选 `all` / `video` / `image` / `file`；`extension` 只在 `type=file` 时生效。返回的 `tempFiles[]` 有 `path`、`size`、`name`、`type`（`video` / `image` / `file`）、`time`，其中 `time` 开发者工具不支持。→ **用 `type: "all"`，拿到后按 `type` 和文件名后缀自己判断**，不用 `extension` 过滤，否则用户选了 PDF 也收不到明确提示。PC 端微信有反馈说选文件没反应，要真机验证。
- `wx.chooseMedia`：`mediaType: ["image"]`，`sourceType: ["album","camera"]`，`sizeType: ["compressed"]`，返回临时路径；需要用户同意隐私协议后才能用（书稿页已在用）。
- `wx.compressImage`：`quality`（只对 JPG 生效）、`compressedWidth` / `compressedHeight`。基础库版本和 iOS/Android 表现以真机为准，低版本要兜底。
- **云存储安全规则**：可用变量有 `auth.openid`、`resource.openid`、`resource.path`。只有上传者能读写的写法：`{"read": "resource.openid == auth.openid", "write": "resource.openid == auth.openid"}`。**控制台和云函数始终有全部文件的读写权限，规则只管小程序端发起的请求。**
- **临时链接**：云函数用 `cloud.getTempFileURL` 生成（`storyImages` 已在用，`maxAge` 设 2 小时）。CloudBase 文档说「传统模式下 TTL 传入无效」。**临时链接到底多久失效、拿到链接的人能否打开，都要实测**，实测前不对外说「链接 2 小时失效」。
- 核对过：小程序端代码目前**没有**直接读写云存储文件（没有 `wx.cloud.uploadFile` / `downloadFile` / `getTempFileURL` 调用），生成图由 `storyImages` 云函数发临时链接。所以把存储规则收紧为只有上传者能读写，不会弄坏现有功能。

依据：微信开放文档中 [wx.chooseMessageFile](https://developers.weixin.qq.com/miniprogram/dev/api/media/image/wx.chooseMessageFile.html)、[wx.chooseMedia](https://developers.weixin.qq.com/miniprogram/dev/api/media/video/wx.chooseMedia.html)、[文件系统](https://developers.weixin.qq.com/miniprogram/dev/framework/ability/file-system.html)、[wx.compressImage](https://developers.weixin.qq.com/miniprogram/dev/api/media/image/wx.compressImage.html)、[云开发计费](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/billing/price.html)；CloudBase 文档 [存储安全规则](https://docs.cloudbase.net/storage/security-rules)。

## 三、照片上云：存储结构与权限

### 3.1 每张照片存两份

| 份 | 规格（**清晰度待定，见第十四节 D1**） | 用途 |
|---|---|---|
| 显示图 `display` | JPEG，长边 ≤ 1600，质量 80（约 200–500 KB） | 书稿和记忆里显示、换手机后恢复 |
| 小图 `small` | JPEG，长边 ≤ 768，逐步降质量到 ≤ 100 KB | 列表缩略图、看图写一句话（参考图出图改用显示图，问题五 09-15 定） |

两份都在手机上用 `wx.compressImage` 压好再上传，云函数里不装图片处理库。**原图不上传。** 手机本机保存的那份保持现状，不删。

### 3.2 云存储路径

```
user-photos/<familyId>/<photoId>/display.jpg
user-photos/<familyId>/<photoId>/small.jpg
```

- `photoId` 沿用现有格式 `photo-<时间>-<随机>`，**不变**。书稿 `content` 里仍然只存 `{ photoId }`，旧客户端不受影响。
- 路径由 `photoId` 决定：同一张照片重传会覆盖同一个位置，不会传出重复文件。补传可以中断和重试，就靠这一点。
- 按 `familyId` 分目录：删除一个家庭时，能一次找全它的照片。

### 3.3 新集合 `photos`（photoId 到云端文件的对应关系）

```
_id: "<familyId>__<photoId>"
{
  familyId, photoId,
  _openid,                     // 云数据库自动写入，即上传者
  displayFileID, smallFileID,
  width, height, displayBytes, smallBytes,
  source: "book" | "import" | "backfill",
  createdAt, uploadedAt,
  deletedAt?                   // 用户在「我的」里删掉云端照片时写入
}
```

- 数据库权限：**仅创建者可读写**。
- 先传文件，再写记录。有记录，才算这张照片「已在云端」。

### 3.4 权限规则（**需要用户同意后在控制台改，我不改**）

- 云存储规则改为：`user-photos/` 下只有上传者能读写；其他路径维持现状（先在控制台看清当前规则，再给出完整规则文本请用户确认）。
- 本人读自己的照片：小程序端直接 `wx.cloud.downloadFile`，规则放行。
- **家人读照片：** 不开放小程序端直接读。新云函数 `photoAccess`（超时设 10 秒）按 `{ familyId, photoIds }` 核对：调用者能看到至少一条引用了这张照片的记忆（`photoIds` 里有它），才返回显示图的临时链接。
  - 判断「能看到这条记忆」：照抄 `familyInvite/core.js` 的 `visibleMemoriesForAccess` 那条判断（房主全部可见；否则是作者本人，或者是 `scope=personal` 且 `sharedWithMemberIds` 里有这个人）。云函数之间没有公共代码层，所以代码注释里写明「须和 familyInvite/core.js 的 visibleMemoriesForAccess 保持一致」。（问题三 09-15 确认）
  - **第一版不做「书稿版本」这一条。** 现在受邀家人本来就看不到任何书稿版本（`familyInvite` 的 `loadRoom` 对非房主返回空的 `manuscriptRevisions`），版本可见范围还没有规则，归问题四定。所以只出现在书稿里、不在任何记忆里的照片，家人第一版看不到。问题四定了规则再加这一条，不自己假设。
  - 受邀家人自己上传的照片，存在房主家庭的目录 `user-photos/<房主 familyId>/` 下，文件归上传者（存储规则按上传者核对）。房主重置房间时一并删除。
- 不做章节级授权，不识别人脸，不判断照片里是谁。

### 3.5 读取顺序（新客户端）与兼容

`readPhoto(photoId)`：

1. 本机原图：现有 `readLocalPhoto`，完全不变。
2. 本机缓存：`USER_DATA_PATH/cloud-<photoId>.jpg`，也登记在 `shiguang-local-<photoId>` 下，所以旧逻辑也能读到。
3. 云端：本人用 `downloadFile(displayFileID)` 下载后写进缓存；非本人调 `photoAccess` 拿临时链接，**只显示、不缓存**。这样收回分享后立刻看不到，和现在记忆的权限行为一致（问题三 09-15 同意）。
4. 都没有：先调 `logLoadError("book-photo", error)` 记进实时日志（问题六 `fix/review-load-error@6197f5e` 的 `services/loadErrorLog.ts`；它进 main 前要用就 cherry-pick 这个提交，不另写），再显示「这张照片没加载出来 · 点一下重试」，不悄悄变成空白。这句文案和样式先问问题八。

- 缓存文件单独计数，超过 100 MB 时从最久没用的删起；**只删缓存，不删本机原图**（本机上限 200 MB）。
- **旧客户端**（体验版和已发出的版本）：书稿里还是 `{ photoId }`，读不到本机文件时照旧显示「【本机照片：…】」占位，保存时占位会还原成 `photoId`（`contentFromDelta` 已经这样处理），不崩也不丢引用。旧客户端插入的新照片不会上传，新客户端下次打开时由补传流程补上。

### 3.6 新照片的上传时机

插入照片或导入照片时：先照现在的做法存到本机，界面立刻显示；同时放进上传队列（`shiguang-photo-upload-queue`，存在本机缓存里），后台上传。

- 队列里每张照片记录状态：`waiting` 等待、`uploading` 上传中、`failed` 失败（附次数和原因）、`done` 完成。
- 小程序回到前台、网络恢复时继续传。失败的照片隔一段时间再试，同一次打开小程序最多试 3 次。
- 界面显示（界面由问题八做）：照片角上一个小状态「正在存到云端 / 没存上 · 点一下重试」；「我的」页显示「还有 N 张照片没存到云端」。
- 书稿能不能保存，不取决于照片有没有上传完：文字照常保存，照片慢慢补。

### 3.7 统一的「读取照片」接口（问题五通过它读照片，**待和问题五定稿**）

所有要读云端照片的地方都走同一个云函数 `photoAccess` 的 `read` 动作，权限只在这一处核对，不在各处各写一份。

**入参**

```
{
  action: "read",
  familyId: string,
  photoIds: string[],              // 1–9 个；ai-reference 最多 3 个
  variant: "small" | "display",    // 看图起草用 small，参考图出图用 display
  purpose: "view" | "ai-caption" | "ai-reference",
  // 只有云函数之间调用时才带：
  onBehalfOfOpenid?: string,
  internalToken?: string
}
```

**身份**

- 小程序端调用：只认 `getWXContext().OPENID`，忽略入参里的 `onBehalfOfOpenid`。
- 云函数之间调用（例如 `storyImages` 替用户读照片）：调用方先用自己拿到的 OPENID 核对过用户，再把它作为 `onBehalfOfOpenid` 传过来。同时要满足两条，才接受这个身份：`SOURCE` 不是 `wx_client` / `wx_devtools`，并且 `internalToken` 等于环境变量 `PHOTO_ACCESS_INTERNAL_TOKEN`（两个云函数的环境变量里各配一份，不写进代码和文档）。云函数之间调用时 OPENID 会不会自动传过来、`SOURCE` 实际是什么值，都要实测。

**权限**

| purpose | 谁能读 |
|---|---|
| `view`（显示） | 上传者本人；家人按问题三的房间级规则（**待对齐问题三**） |
| `ai-caption`、`ai-reference`（发给模型） | **只有上传者本人**（`photos._openid` 等于调用者），不要求是家庭主人：受邀家人可以让 AI 看自己上传的照片，但房主不能把家人上传的照片发给模型。把照片发给模型只能由上传者决定；「存到云端」和「发给腾讯云 TokenHub 做 AI 处理」是两次单独同意（个保法第 23 条），后者由问题五在调用前取得，本接口不代替它问 |

**返回**

```
{
  photos: [{
    photoId,
    status: "ok" | "not_uploaded" | "deleted" | "forbidden" | "not_found",
    contentType?: "image/jpeg", width?, height?, bytes?,
    url?: string         // 临时链接
  }]
}
```

- **只返回临时链接，不返回 base64**（问题五 09-15：TokenHub 支持图片链接，它的质检已经这样用）。代价是链接会被服务商拿去下载，这一点写进看图和参考图的单独同意里。

- 每张照片单独给状态，一张失败不影响其他照片。`not_uploaded` 表示手机里有，但还没传到云端，调用方提示用户稍后再试。
- **不返回 fileID**，避免调用方拿到 fileID 后绕过这里的权限核对。
- 超时设 10 秒。临时链接的实际有效期实测后写进本节。
- 这个云函数**只读照片**，不调用任何模型、不写日志以外的数据。

**在照片上云做完之前**：问题五按上面的返回格式，在自己的测试里用虚构测试图做替身，不连真实的 `photos` 集合。要用我的实现时，把本分支合进它的分支，不在两边各写一份。

## 四、手机里已有照片的补传

1. **找照片**：遍历本账号所有书稿版本（含历史版本）里的 `photoId` 和「【本机照片：…】」占位，去掉 `photos` 集合里已经有记录的。插入过、但从没保存进任何版本的照片不补传。
2. **征得同意**（单独弹窗，文案待问题七定）：说清找到几张、大约多大、存到哪里（微信云开发云存储）、谁能看（本人；家人按邀请权限）、怎么删（「我的 → 云端照片 → 删除」）；按钮是「补传」「先不」。选「先不」时，「我的」页留一个入口，之后不再主动弹。
3. **补传**：走第 3.6 节的同一个队列，显示「已存 12 / 40 张」，可以「暂停」。离开页面或退出小程序后，下次接着传。
4. **找不到的照片**：本机文件已经没了（换过手机、清理过小程序），列出「这台手机上找不到 N 张」，不报错、不重试。换回原来那台手机补传就行。
5. **不重复**：路径由 `photoId` 决定，而且只补没有记录的照片。
6. **不碰真实数据的测试**：用虚构家庭加开发者工具测完整流程（中断、重试、找不到文件、重复触发），再请用户用真机体验版确认。动真实照片前，先说明影响和回退办法，征得同意。

## 五、导入流程

### 5.1 入口

聊天页底部「导入」，点开后弹 `wx.showActionSheet`：「从相册选或拍照」「从微信聊天里选文件」。导入**另存一条记忆**，不打断正在进行的聊天。存好后提示「已存进『还没写进书』」。

### 5.2 类型判断

| 选到的东西 | 怎么处理 |
|---|---|
| 图片（相册、拍照，或聊天文件里 `type=image` 的） | 最多 9 张 → 5.4 写一句话 |
| `.txt`、`.md` | 读正文 → 5.3 |
| `.doc`、`.docx`、`.pdf`、`.wps`、`.pages` | 提示「Word、PDF 这一版还读不了。可以打开文件复制文字，粘贴到聊天框里。」 |
| 视频、其他文件 | 提示「这一版只能导入图片和 .txt、.md 文字文件。」 |

- 一次导入存成**一条**记忆：最多 1 个文字文件，加最多 9 张图片。选了多个文字文件时，提示「一次选一个文字文件」。
- 可以导入的和不支持的混在一起选时，只存能导入的，再告诉用户哪几个没导入。

### 5.3 文字文件

- 文件大于 1 MB 就不读，提示「文件太大」。
- 用 `readFile` 按 `utf-8` 读，去掉开头的 BOM。读出来含乱码替换符（`�`）时，提示「这个文件不是 UTF-8 编码，读出来会是乱码。可以用手机备忘录另存一次再导入」，不存乱码。
- `.md` 做轻量清理：去掉标题前的 `#`，图片 `![...](...)` 整个去掉，链接 `[文字](地址)` 只留文字。其余原样保留。
- 标题取文件名去掉后缀，截到标题上限。
- **分段（按用户选的 B）**：
  - 500 字以内，是只有一段的普通记忆。
  - 超过 500 字，按段落切，每段不超过 500 字。单个段落本身超过 500 字时，在句号、问号、叹号、分号处断开；实在没有标点，才按 500 字硬切（沿用 `splitRecoverableText` 按码点切，不切坏 emoji）。
  - 段的来源记为 `"import"`（**待问题四确认**）。
- 这一版一次最多导入 2 万字（约 40 段，与整本书稿 2 万字上限一致）。超过就提示分成几个文件，不自动截断。
- ⚠️ 现在的 `normalizeMemoryText` 会把所有空白压成一个空格，段内换行会丢。导入的段内是否保留换行，**待问题四确认**。

### 5.4 只有图片：写一句话

- 弹层（界面由问题八做，输入框和保存逻辑归问题九）：照片缩略图、一个输入框（不超过 500 字）、「跳过」「保存」。「让 AI 看看照片，帮我起个头」这个按钮后面接的是问题五的能力（第六节）。
- 选「跳过」：存成只有照片、文字为空的记忆。标题默认「照片 · 9月15日」。
- ⚠️ 现在的 `createContribution` 和 `familyInvite` 云函数都**不接受空文字**，需要改成「文字和照片至少有一样」。旧客户端读到文字为空的记忆会不会出问题，要用虚构数据实测（初步看读取时不按文字过滤）。**待对齐问题四。**

### 5.5 记忆怎么挂照片（**待问题四确认字段**）

```
MemoryContribution.photoIds?: string[]   // 最多 9 个，格式同书稿 photoId
```

- 照片挂在整条记忆上，不挂在某一段上。接着讲时可以再加照片，但总数不超过 9 张。
- 写进章节时，这条记忆的照片作为候选插进章节（一本书稿最多 9 张的规则不变）。具体怎么插，由问题四、问题八定。
- `familyInvite` 云函数 `normalizeContributionInput` 的字段白名单要加上 `photoIds`，否则存进共享故事时照片会被丢掉。

### 5.6 合并顺序

- 只有照片、以及 500 字以内的文字：只依赖「记忆挂照片」字段，可以早做。
- 超过 500 字的文字：依赖问题四 `feat/story-records` 先合进 `main`，并提供「用多段新建记忆」的函数（现在只有 `appendMemorySegment`）。**我不在本分支重写分段逻辑。**
- 聊天页底部的改动，排在问题八 `feat/ui-clarity` 合进 `main` 之后，免得两边改同一块。

## 六、看图写一句话（**问题五负责**，本节只记和问题九的交界和已有设想）

> 协调会话 09-15 分工调整：这一块整体归问题五。问题九只负责：写一句话的输入框和保存；通过 3.7 节的接口提供照片（`purpose: "ai-caption"`，`variant: "small"`）；把问题五给回的草稿填进输入框，存进记忆时带上它给的 AI 标识字段。下面的设想交问题五参考，以问题五的方案为准。

- **触发**：只有用户点「让 AI 看看照片，帮我起个头」才发，不自动识别。
- **单独同意**（和文字 AI 授权分开）：写明发给谁（腾讯云 TokenHub 上的看图模型）、发什么（这几张照片的小图）、用来做什么（只起草一句话），不识别照片里的人是谁；拾光家忆只保存用户最后确认的那句话，不另存分析结果；服务商的日志政策仍适用。**文案和同意范围待问题七定**（每次问，还是每次打开小程序问一次）。
- **调用**：由问题五在 `storyImages` 里实现：把看图调用抽成 `vision.js`，再写 `caption`。照片通过 3.7 节 `photoAccess.read` 读取（`variant: "small"`，临时链接）。次数单独计，不占出图名额。
- **标识（问题七 09-15/16 定）**：只要记忆里有照片，AI 文字一律标「文字 AI 生成」，用户改过标「文字 AI 生成 · 已由你修改」，免得被误以为照片是 AI 画的。用户把草稿整个删空、自己重写的不标。
- **提示词要点**：用一句中文（不超过 60 字）写照片里看得见的场景、物件、光线、季节和年代线索；不猜人物身份、姓名、关系，不描述长相；看不清就说看不清，不编。
- **结果**：填进输入框作为草稿，框上标「AI 生成」。用户改过再保存时，标识按问题七的规范（参照「文字 AI 生成 · 已由你修改」），**待问题七定**。存进记忆时，这一段的 `organizationMode` 记为 `"cloud-ai"`。
- **失败兜底**：超时（15 秒）、没通过审核、服务没配置时，提示「没看出来，自己写一句吧」，输入框保持用户原来的内容，不自动重试。每人每天最多用 30 次（**待定**）。
- **过审**：拿到「深度合成」类目前可以开发，在体验版里用，不提审；不做「审核时隐藏 AI」的开关。

## 七、参考图出图（阶段 4，问题五主做）

- 问题五原方案 7.1 是「手机上压到 ≤ 100 KB，base64 随调用发给云函数，不存云端」。照片上云后改为：`storyImages` 通过 3.7 节 `photoAccess.read` 读**显示图**的临时链接（`purpose: "ai-reference"`，`variant: "display"`，最多 3 张，已删除的跳过），不再从手机传图。100 KB 的限制原本只是为了随调用传图，现在不需要了（问题五 09-15 定，它的方案已在 `feat/image-reference-photos@6e65573` 更新 7.1）。图片任务里只记 `photoIds` 和张数，不存照片内容。
- 我提供：3.7 节的读取接口和小图规格。
- 保持问题五的护栏：「识别长相」单独开关，默认关闭；用户自己挑选只有场景和物件的照片作参考图；不自动判断照片里有没有人脸、是谁。

## 八、删除与恢复

| 用户做了什么 | 云端照片怎么办 |
|---|---|
| 从书稿或记忆里去掉一张照片 | 只去掉引用；文件保留，因为历史版本可能还用着，恢复旧版本时还能显示 |
| 删除记忆或故事（现在是软删除，可恢复） | 文件保留，恢复后照片还在 |
| 「我的 → 云端照片 → 删除云端照片」 | 删掉本人所有 `user-photos/` 文件和 `photos` 记录，两次确认；本机原图不动；别的手机和家人那边之后显示「照片已删除」 |
| 清空或重置房间（`resetCurrentUserRoom`），删演示家庭（`deleteDemoFamilyOnce`） | 加上 `photos` 集合和 `user-photos/<familyId>/` 下的文件，不留孤儿文件；`inspectFamilyData`、`ensureCloudCollections` 同步加上 |

- 「我的」页显示：「云端照片 N 张 · X MB」「还有 N 张没存到云端」。
- 没被任何记忆或版本引用的照片（插了但没保存），第一版不自动清理，只计入占用；第二版再做清理。

## 九、费用与压缩

- 估算：每张显示图约 400 KB，小图约 80 KB，合计约 0.5 MB。1000 张约 0.5 GB。
- 云开发计费页写的是：基础套餐含 2 GB 容量、每月 5 GB CDN 流量；超出后按量计费。**具体单价以控制台为准，上线前核一次。**
- 省流量：本人下载后缓存在本机，不重复下载；家人按需拿临时链接；列表只用小图。
- 包体积：不新增图片素材，新代码预计几十 KB 以内；每个阶段结束时用开发者工具看一次主包大小。

## 十、隐私保护指引与文案改动清单（交问题七）

**微信后台《用户隐私保护指引》：**

1. 新增「选择微信聊天中的文件」（`wx.chooseMessageFile`）：用于把聊天里的文字文件和图片导入成记忆。
2. 修改「照片」条目：从「只保存在手机本机」改为「上传到微信云开发云存储，用于在你的书和记忆里显示、换手机后恢复；默认只有你能看，家人按你的邀请权限查看；可在『我的』里删除」。
3. 第三方：腾讯云 TokenHub 看图模型，只在用户单独同意、点「让 AI 看看照片」时，才接收照片小图。
4. 相册或摄像头（`wx.chooseMedia`）条目核对是否已有。
5. 改完重新提审。**AI 部分要等企业主体和「深度合成」类目。**

**代码和文档里要改的承诺（措辞由问题七和用户定稿）：**

- `miniprogram/services/aiConsent.ts`：「不发送照片」「照片文件仅在本机」。文字 AI 仍然不发照片，但存储说法要改。
- `miniprogram/pages/me/me.wxml:71`「照片文件仅保存在本机…」、`:121`「不发送照片」。问题八 `feat/ui-clarity` 也改了 `me.wxml`，改前先对齐。
- `miniprogram/pages/book/book.ts` 插照片后的提示「照片仅在本机；请点保存。换手机或清理小程序后不可恢复。」
- `HACKATHON_COMPLIANCE.md`「赛后变化」里「照片文件仍只保存在手机本机，快照只带照片引用」；「第三方依赖」补上云存储照片和看图模型。
- `docs/handoff/2026-09-14-stories-to-desktop.md` 第 7 行。
- `docs/2026-09-13-image-management-plan.md` 7.1「拾光家忆自己不保存照片」（问题五的文档，由问题五改）。

**新增弹窗（文案交问题七）：** 补传同意、看图同意、删除云端照片的两次确认。

## 十一、对「发到电脑端」接口约定的影响（交问题一）

- **这一版不改**：快照仍只带 `photoId` 引用，接口约定 1.0.0 不升版本。电脑端显示「照片在手机上」。
- 以后如果要带照片：由 `drinkingTimeBridge` 在服务端生成临时链接或直接转交文件。这是只加字段的改动，可以升到 1.1.0。权威版本在 Drinking Time 仓库，由「问题一：联动·网页端」维护。**要不要带、什么时候带，之后单独问用户（D3）。**

## 十二、分工与待对齐

| 会话 | 待对齐 |
|---|---|
| 问题四 | ① `MemorySegmentSource` 加 `"import"`；② 用多段新建记忆的函数；③ `photoIds` 字段；④ 允许文字为空、只有照片的记忆；⑤ 段内换行是否保留；⑥ `familyInvite/core.js:79`（超过 500 字拒绝）和 `generateBiography/index.js:24`（只读前 500 字）谁来改；⑦ 合并顺序 |
| 问题八 | 「导入」入口、类型提示、写一句话弹层、照片上传状态和失败重试、「我的 → 云端照片」、补传进度；它的 `feat/ui-clarity` 改了 `book.ts`、`me.wxml`、聊天页样式，合并顺序 |
| 问题五 | **09-15 已回复并按它的意见改**：只返回临时链接；参考图用显示图，最多 3 张；发给模型的照片只看上传者；它方案 7.1 已更新（`feat/image-reference-photos@6e65573`）。**还在等**：能不能接受云函数之间用 `onBehalfOfOpenid` 加 token 的调用方式。它要用我的实现时，把本分支合进它的分支 |
| 问题七 | 第十节全部（照片上云、选聊天文件、补传弹窗、删除确认）；看图同意和 AI 草稿的标识改由问题五去问 |
| 问题三 | **09-15 已回复**：照抄 `visibleMemoriesForAccess` 判断记忆可见；版本级可见范围不存在，第一版不做；家人不缓存照片 |
| 问题一 | 第十一节：这一版不升版本。**09-15 已确认无异议**（photoId 只作占位，服务端不获取、不等待）；带不带照片等 D2 定了再找它和网页端 |
| 问题六 | **09-15 已回复**：没有通用失败组件，失败时先 `logLoadError`（`6197f5e`），提示文案归问题八；它不碰 `book.ts` 读照片的地方，不用排先后 |

## 十三、分阶段

- **阶段 1：照片上云基础。** `photos` 集合、上传队列、两份压缩、读取顺序、`photoAccess`（含 3.7 节读取接口）、书稿照片走云端、补传、删除云端照片、重置房间时清理、测试。做完告诉问题五可以接云端照片。需要用户做的事：同意并在控制台改存储规则、建集合的数据库权限、部署云函数（每一步单独征得同意）。
- **阶段 2：导入。** 入口、类型判断、文字文件、分段（等问题四）、写一句话输入框、记忆挂照片。
- **阶段 3（问题五）：看图写一句话。** 问题五负责；问题九只把它的草稿接进输入框。
- **阶段 4（问题五）：参考图出图。** 问题五负责；问题九提供读取接口。

## 十四、要问用户的决定（一次一件）

- **D1 云端存多清楚**：显示图长边 1600（每张约 0.4 MB），还是 2560（每张约 1 MB，老照片翻拍放大看细节更清楚，费用约翻倍）。← 09-15 已问，等用户答
- **D2 发到电脑端要不要带照片**，以及什么时候带。
- （看图同意怎么问、看图次数上限，随分工转给问题五。）

## 十五、需要实测的风险

- 临时链接的实际有效期，以及拿到链接的人能否打开。
- `wx.compressImage` 在低版本基础库和 iOS、Android 上是否支持指定宽高。
- `wx.chooseMessageFile` 在 PC 微信上选文件没反应。
- 旧客户端读到文字为空的记忆、带 `photoIds` 的记忆、超过 500 字的分段记忆时的表现（用虚构数据测）。
- 同一张照片在两台手机上同时补传会不会冲突（按设计，同一路径覆盖，结果一致）。
