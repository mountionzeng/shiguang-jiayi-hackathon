@import "../archive/archive.wxss";
.story-content { position: relative; z-index: 1; }
.story-memory { margin: 20rpx 0; padding: 28rpx; text-align: left; background: #fffdf6; border-radius: 12rpx 24rpx 18rpx 10rpx; }
