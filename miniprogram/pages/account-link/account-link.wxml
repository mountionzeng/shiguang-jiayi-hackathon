<view class="link-page"><image class="paper" src="/assets/illustrations/home-paper.jpg" mode="scaleToFill"></image><view class="content">
  <view class="title serif">在电脑上继续</view>
  <view class="note">选择一个微信里的故事，生成五分钟有效的一次性登录码。电脑会进入这个微信账号，并保留故事的记忆、人物、地点和整理好的章节。</view>
  <view class="code-card" wx:if="{{code}}">
    <view class="code-title serif">{{selectedTitle}}</view><view class="code" user-select>{{code}}</view>
    <view class="expires">{{expiresLabel}} · 只能使用一次</view>
    <button class="primary" bindtap="copyCode">复制登录码</button>
    <view class="hint">电脑打开 test.drinkingtime.top/login，选择“微信登录”，填入上面的码。</view>
  </view>
  <view class="section serif">选择要带到电脑的故事</view>
  <button class="story" wx:for="{{stories}}" wx:key="key" data-key="{{item.key}}" data-title="{{item.title}}" bindtap="choose" disabled="{{busy}}">
    <view><text class="story-title">{{item.title}}</text><text class="story-detail">{{item.detail}}</text><text class="story-excerpt">{{item.excerpt}}</text></view><text class="story-arrow">生成电脑码 ›</text>
  </button>
  <button class="load-more" wx:if="{{loadError}}" bindtap="refresh" loading="{{busy}}">{{loadError}}</button>
  <view class="empty" wx:if="{{!busy && !loadError && stories.length===0}}">还没有可以带到电脑的故事</view>
</view></view>
