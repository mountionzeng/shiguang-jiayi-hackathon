# 黑客松合规与来源声明

> 阅读说明（2026-09-14 更新）：「时间线」至「待主办方确认」各节记录的是**参赛期间**的事实，保持原样，不改写。赛后本项目的定位已经变化——它现在是 **Drinking Time 的微信小程序手机端**，见文末「赛后变化」一节。

## 时间线

- 开赛日期：2026-08-27（用户于 2026-08-28 确认“开始时间是昨天”；如主办方另有精确时刻要求，以官方公告为准）
- 独立代码仓库创建：2026-08-28
- 赛前状态：产品想法与需求讨论，没有可运行的“拾光家忆”作品
- 赛中状态：从空仓库开始实现原生微信小程序和独立云函数

## 与既有项目的边界

团队此前存在名为 `Drinking-Time` 的独立项目。本次参赛版本：

- 不复制其源代码、组件、提示词、数据结构、素材或 Git 历史；
- 不调用其现有后端、数据库或线上服务；
- 不把其既有功能计入本次作品成果；
- 仅把“赛后可能整合”列为未来计划。

如果后续发现必须复用任何赛前实现，应在继续前取得主办方书面许可，并在本文件和提交文档中完整披露。

## 第三方依赖

- 微信小程序运行环境与微信云开发
- npm 开发依赖及其各自许可证
- 可选的 OpenAI-compatible 文本模型服务

第三方平台和通用开源依赖不作为团队原创成果申报。最终提交前应再次核对比赛对 AI 服务、云服务和开源依赖的披露要求。

## 数据与隐私

- 黑客松演示只使用虚构家庭、虚构姓名和合成回忆。
- 不在公开仓库、演示视频或项目图片中出现真实老人的私人经历、照片、录音或联系方式。
- 正式产品必须在收集真实家庭材料前补齐明确同意、访问控制、撤回删除和隐私保护指引。
- AI 只能使用已经由老人确认、且投稿者选择家庭可见的材料生成正文；私密投稿不得进入传记或模型请求；演示降级模式必须明确标注，不能冒充真实模型输出。
- 云 AI 默认关闭。真实用户使用前必须补齐服务端微信身份、房间成员授权、权威来源查询、调用配额和请求去重。

## 待主办方确认

建议保留以下问题的书面答复：

> 我们赛前仅有产品想法和需求讨论，没有“拾光家忆”的可运行代码。比赛期间从零创建独立微信小程序和云函数，不复制或依赖既有 Drinking-Time 代码与线上服务；赛后才可能整合。使用微信云开发、通用开源依赖和文本模型 API 是否符合软件赛道的一次开发要求？

## 赛后变化：成为 Drinking Time 的手机端（2026-09 起）

比赛结束后，团队决定不再把拾光家忆当作独立作品运营，而是把它作为 **Drinking Time 的微信小程序手机端**：用户在手机上随时聊一段、记下记忆、整理成故事；在手机上开了头的故事，可以带到电脑上的 Drinking Time 继续制作。

### 已经发生的整合

- **2026-09-13**（首个提交 `fbc6ff6`）：新增 `drinkingTimeBridge` 云函数与账号关联页，曾支持用 Drinking Time 邮箱验证码关联账号，把网页故事单向导入小程序。
- **2026-09-14**（提交 `ea60254`）：按产品方向纠正数据流，改为**手机端 → 电脑端**：在拾光家忆里选择故事、生成一次性电脑登录码，电脑兑换后进入同一微信主体的 Drinking Time 账号继续制作。**旧的邮箱关联与“网页故事导入微信”流程已移除。** 故事快照包含记忆与当前书稿章节；照片文件仍只保存在手机本机，快照只带照片引用，不传照片字节。细节见 [docs/handoff/2026-09-14-stories-to-desktop.md](docs/handoff/2026-09-14-stories-to-desktop.md)。
- **2026-09-16**（分支 `feat/import-and-cloud-photos`）：书稿照片增加微信云开发备份。原图仍留在手机，只上传用于显示和 AI 看图的两份压缩图；默认只有上传者本人可读，受邀家人必须同时拥有对应记忆的阅读权限且平台检查明确通过。用户可在「我的」查看占用并两次确认后删除自己的全部云端照片。聊天页可从相册、拍照或微信聊天导入图片及 UTF-8 的 `.txt`、`.md` 文件，Word、PDF 暂不读取。文字 AI 授权不包含照片；需要 AI 看图时另行询问，并只发送当次选择的压缩图。跨端故事快照目前仍只传 `photoId` 引用，不传照片字节。
- 两端经 `drinkingTimeBridge` 云函数对接 Drinking Time 服务端，**不共享数据库**。
- **2026-09-15**：双方约定线上传输格式并逐条核对，写成 [docs/integrations/drinking-time-bridge-contract.md](docs/integrations/drinking-time-bridge-contract.md)（约定版本 1.0.0，权威版本在 Drinking Time 仓库，用户已确认这版约定）。约定只记录两端之间传输的数据和可观察行为，不包含 Drinking Time 内部的表结构、故事内容结构、提示词或素材；本仓库的副本经核对与权威版本逐字节一致。

### 与参赛版本的边界

- 上述整合全部发生在**赛后**，不属于参赛提交成果；参赛作品的评审范围以比赛截止时提交的版本为准（截止时刻与提交物以主办方记录为准）。
- 因此，上文「与既有项目的边界」描述的是参赛版本，**不再描述本仓库的当前状态**：当前仓库已经依赖 Drinking Time 的线上服务（把故事发送到电脑端继续制作）。
- 如果本仓库仍会作为比赛作品被评审或公开展示，应主动向主办方说明这一赛后变化，并提供比赛截止时的提交版本作为评审对象。
- 今后如果把 Drinking Time 的源代码、提示词、数据结构或素材复制进本仓库，应在本节逐项披露来源与范围。

### 已经过时的参赛期表述

以下参赛期描述已不代表现状，保留仅作记录：「仅把赛后可能整合列为未来计划」（整合已开始）；「云 AI 默认关闭」（赛后已在云函数中开启在线 AI，并加入单独授权）。
